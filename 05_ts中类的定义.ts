/*class Person {
    name: string;//属性 前面省略了public关键词
    constructor(n: string) {
        this.name = n;
    }
    run(): void {
        alert(this.name + '在运动');
    }
    getName(): string {
        return this.name;
    }
    setName(name: string): void {
        this.name = name;
    }
}
const p = new Person('张三');
p.run();

p.setName('李四')

let personName: string = p.getName();
console.log(personName);
*/

